<%= form_for(@hdd) do |f| %>
  <% if @hdd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hdd.errors.count, "error") %> prohibited this hdd from being saved:</h2>

      <ul>
      <% @hdd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
